# Playwright Test Environment Variables
# Copy this file to .env in the tests/playwright directory for local testing

# Base URL for tests (optional, defaults to http://localhost:3000)
PLAYWRIGHT_BASE_URL=http://localhost:3000

# Test user credentials (for authenticated tests)
# Note: These should be test accounts, not production accounts
TEST_USER_EMAIL=test@example.com
TEST_USER_PASSWORD=TestPassword123!

# API endpoints (if testing against different environments)
# TEST_API_URL=http://localhost:3000/api

# Enable debug mode
# DEBUG=pw:api

# Slow down operations for debugging
# PLAYWRIGHT_SLOWMO=100
